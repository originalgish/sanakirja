{
  "name": "sanakirja",
  "version": "1.0.0",
  "author": "DenisM",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"yarn workspace server dev\"  \"yarn workspace client dev\"",
    "build": "concurrently --kill-others-on-fail \"yarn workspace server build\"  \"yarn workspace client build\""
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
